package org.avricot.rating.repository.rating;

import org.avricot.rating.model.rating.RatingPropertyValue;
import org.springframework.data.repository.CrudRepository;

public interface RatingPropertyValueRepository extends CrudRepository<RatingPropertyValue, Long> {
}
